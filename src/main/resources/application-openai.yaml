spring:
  ai:
    retry: 2
    openai:
      api-key: ${LLM_API_KEY}
      base-url: ${API_URL}
      chat:
        options:
          model: glm-4.6
          # 启用 GLM-4.6 的思考模式
          # extra-body 中的参数会作为顶层参数发送到 API
          extra-body:
            enable_thinking: true
