# 快速参考卡片

## 🚀 Maven Profile 快速切换

### 命令行方式

```bash
# 使用智谱AI（默认）
mvn spring-boot:run
mvn spring-boot:run -Pzhipu

# 使用 Gemini
mvn spring-boot:run -Pgemini

# 构建 JAR
mvn clean package -Pzhipu
mvn clean package -Pgemini
```

### 使用脚本（推荐）

#### Windows
```cmd
# 运行应用
doc\sh\run-with-profile.bat zhipu
doc\sh\run-with-profile.bat gemini

# 构建应用
doc\sh\build-with-profile.bat zhipu
doc\sh\build-with-profile.bat gemini
```

#### Linux/Mac
```bash
# 运行应用
./doc/sh/run-with-profile.sh zhipu
./doc/sh/run-with-profile.sh gemini

# 构建应用
./doc/sh/build-with-profile.sh zhipu
./doc/sh/build-with-profile.sh gemini
```

### IDEA 中切换

1. 打开右侧 Maven 面板
2. 展开 `Profiles` 节点
3. 勾选 `zhipu` 或 `gemini`
4. 点击刷新按钮

## 📁 配置文件说明

| 文件 | 说明 |
|------|------|
| `application.yaml` | 主配置文件，指定激活的 profile |
| `application-zhipu.yaml` | 智谱AI配置 |
| `application-gemini.yaml` | Gemini配置 |

## 🔧 添加新 Profile

### 1. 创建配置文件
在 `src/main/resources/` 创建 `application-{name}.yaml`

### 2. 在 pom.xml 添加 profile
```xml
<profile>
    <id>name</id>
    <properties>
        <spring.profiles.active>name</spring.profiles.active>
    </properties>
</profile>
```

### 3. 使用新 profile
```bash
mvn spring-boot:run -Pname
```

## 📚 详细文档

- [Maven Profile 使用指南](maven-profile-usage.md)
- [项目指南](../CLAUDE.md)

---

**提示**: 默认使用 `zhipu` profile，无需显式指定。
